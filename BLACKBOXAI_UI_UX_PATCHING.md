# UI/X Live Patch Tracker

## Started
- Purpose: Fix live contrast (buttons), refactor hero to luxury Bento grid, and apply cinematic depth/glass across globals + header/footer.

## Current Repo State
- Partial changes applied earlier to `src/app/layout.tsx` and `src/app/page.tsx` only.
- Pending full implementation:
  - `src/app/globals.css`
  - `src/components/site-header.tsx`
  - `src/components/site-footer.tsx`
  - Finish `src/app/page.tsx` to the requested 12-col Bento grid + hotline badge + reassurance line.

## Next
1) Update `globals.css` with vignette/lens + reusable classes.
2) Update header/footer with premium contrast button styles.
3) Refactor hero page layout to Bento grid + hotline badge + hover tilt.
4) Run build + redeploy.

